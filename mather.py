# Takes a left and right param, returns results
def mather(left, right):
    sum = left + right
    difference = left - right
    product = left * right
    quotient = left / right

    return (sum, difference, product, quotient)